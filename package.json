{
  "name": "kevas",
  "version": "3.0.1",
  "description": "Stream replacing {{keys}} with values",
  "main": "lib",
  "bin": "bin/kevas.js",
  "author": "Eli Doran <eli+npm@elidoran.com>",
  "license": "MIT",
  "keywords": [
    "transform",
    "kevas",
    "key",
    "keys",
    "value",
    "values",
    "stream",
    "replace",
    "low memory",
    "mustache",
    "template",
    "handlebars",
    "mustache"
  ],
  "scripts": {
    "lint": "coffeelint -f build/coffeelint.json lib",
    "build": "coffee --bare --compile --no-header lib bin",
    "clean": "rm -rf lib/*.js bin/command.js",
    "prepublish": "npm run build",
    "postpublish": "npm run clean",
    "test": "_mocha --compilers coffee:coffee-script/register --reporter spec --bail --check-leaks test/lib",
    "precoverage": "npm run clean",
    "coverage": "istanbul cover -x 'benchmark/**' -x 'build/**' -x 'docs/**' -x 'examples/**' _mocha -- --compilers coffee:coffee-script/register --require coffee-coverage/register-istanbul --require build/coverage.js -R spec test/lib",
    "precoverage-lcov": "npm run clean",
    "coverage-lcov": "istanbul cover -x 'benchmark/**' -x 'build/**' -x 'docs/**' -x 'examples/**' _mocha --report lcovonly -- --compilers coffee:coffee-script/register --require coffee-coverage/register-istanbul --require build/coverage.js -R spec test/lib",
    "devrun": "coffee bin/kevas.js --"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/elidoran/node-kevas.git"
  },
  "bugs": "http://github.com/elidoran/node-kevas/issues",
  "devDependencies": {
    "coffee-coverage": "^2.0.1",
    "coffee-script": "^1.12.6",
    "coffeelint": "^1.16.0",
    "coveralls": "^2.13.1",
    "fspath": "^0.14.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.1",
    "mocha-lcov-reporter": "^1.3.0",
    "strung": "^1.1.5",
    "through2": "^2.0.3"
  },
  "dependencies": {
    "chain-builder": "^0.13.2",
    "nuc": "^0.4.0",
    "stream-search-helper": "^0.7.1",
    "value-store": "^0.3.6"
  }
}
